# cov_shift_intent_datasets
Two new intent classification benchmarks that, along with out-of-scope utterances, contains in-scope utterances sampled from different distributions not seen during training.
